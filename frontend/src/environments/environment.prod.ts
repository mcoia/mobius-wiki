// Production environment configuration
export const environment = {
  production: true,
  apiUrl: '/api/v1',  // Relative URL for production (same origin)
  backendUrl: ''  // Empty for same origin
};
